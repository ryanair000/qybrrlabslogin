import React from 'react';

// A simple layout for the profile section that doesn't include Sanity preview logic
export default function ProfileLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>; 
} 