import React from 'react';

// A simple layout for the search section that doesn't include Sanity preview logic
export default function SearchLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>; 
} 